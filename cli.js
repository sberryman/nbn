// to-do
// placeholder for client library
// which includes a command line parser
// possible an env parser
// then simply wraps index with the parsed config
// should probably be run with a process monitor like pm2 (https://www.npmjs.com/package/pm2)

var Lib = require('./index');
